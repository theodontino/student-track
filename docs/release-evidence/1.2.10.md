# Student Track 1.2.10 Zhuiver 发布记录

- Previous Student Track version: 1.2.9
- Student Track version: 1.2.10
- Zhuiver level: PATCH
- Zhuiver rationale: 在既有“录入—规划—生成”三步、FeedbackPlan 与 FeedbackPlanBatch 模型上补齐计划命名、草稿保存、全过程回看和不可覆盖的来源修订，修复生成后无法查看采用材料与计划、原位修改边界不清等真实使用阻断；不新增反馈任务实体，也不改变导出、批准或跨仓协议。
- Product claim changed: no
- Core workflow changed: no
- Domain model changed: yes
- Protocol contract changed: no
- Protocol issue/tag (if applicable): not applicable
- Compatibility records: 历史未命名 Plan/Batch 继续使用自动标题；FeedbackPlanInputSnapshotV1 继续读取；1.2.9 深链接和 `/api/feedback/tasks` 保留兼容；新增 V2 快照与可空来源关系不回填旧数据。
- Verification evidence: `npm run verify:release` 已通过全部 11 个步骤；覆盖率阶段 93 个文件、580 项测试通过，默认 WebKit 49 项通过，生产构建通过。命名计划相关 Service/API/状态定向测试 121 项通过，反馈工作台整文件 WebKit 12 项通过；另以完整课程周期隔离库运行多班课程周期与教师纯页面专项，3 项通过。全新迁移链、固定旧库、固定 1.2.9 Plan/Batch/V1 快照库与真实数据库副本升级检查通过，真实 SQLite 主文件与 WAL 前后未变化。

## Evidence

- [x] 单班与多班命名草稿自动保存、手动保存、`Command-S` 和刷新恢复通过
- [x] 生成阶段可切换查看录入来源、完整计划并用 `view` 深链接恢复
- [x] 生成后配置冻结；修正生成新计划且旧正文、批准和导出保持不变
- [x] 沿用冻结事实与按当前事实新建的入口和快照不混用
- [x] 历史 V1 快照、未命名计划、1.2.9 深链接和 Task API 兼容
- [x] Prisma 迁移已在全新数据库、固定 1.2.9 合成库和真实数据库升级副本验证
- [x] `npm run verify:release` 通过：11 个步骤，覆盖率阶段 580 项测试，默认 WebKit 49 项；课程周期专项 WebKit 3 项另行通过
- [x] `npm run privacy:check`、`npm run docs:check` 和 `npm run docs:links` 通过
- [x] 真实 SQLite 主文件与 WAL 未变化
- [x] 未修改跨仓协议、WCG no-send 或 STEP experimental 边界
