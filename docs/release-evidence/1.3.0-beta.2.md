# Student Track 1.3.0-beta.2 Zhuiver 发布记录

- Previous Student Track version: 1.3.0-beta.1
- Student Track version: 1.3.0-beta.2
- Zhuiver level: MINOR
- Zhuiver rationale: 在 beta.1 的反馈计划主体、回收站和事实冻结工作流上，增加同源 Core / Full 构建身份、Windows Core 运行路径、学期公共材料 Excel 整库导入与同号讲次草稿，属于向后兼容的产品能力扩展。
- Product claim changed: yes
- Core workflow changed: yes
- Domain model changed: yes
- Protocol contract changed: no
- Protocol issue/tag (if applicable): not applicable
- Compatibility records: Core 与 Full 共用 beta.1 的 Prisma Schema、migration 和业务数据；未修改 `docs/contracts/**`、WCG wire shape 或协议仓。macOS 默认 Full；Windows 10/11 x64 只提供 Core，全部录音转写（包括本地 FunASR、通义听悟和阿里云 ASR）与 WCG 在页面、动作和 API 统一阻断，Windows 脚本不探测、不启动也不调用这些 Full 能力。对应 GitHub Issue #57 和 #58。
- Verification evidence: 已发布的 `v1.3.0-beta.2` annotated tag 固定指向 `14993b22b447b801171907f4299985b0c5265ea3`；该提交的 quality、documentation、WebKit browser 与 Windows Core CI 均通过。PR #59 在 tag 后继续完成 Windows 双击安装、离线交付物和 CI 职责收口；交付实现提交 `fdfde694438a5b856a849321584606d10ae7b01a` 的 macOS Full/Core quality、Full/Core Chromium、WebKit、documentation 与 Windows Core delivery 七项检查全部通过。Windows runner 成功组装并解压离线 ZIP，在隔离的 `LOCALAPPDATA` 中安装、生产启动、确认只监听 `127.0.0.1`，创建学期后停止并重启，再按原 stable ID 读取。本地 `npm run verify:quick` 通过 102 个测试文件、658 个测试和真实数据库保护，`npm run verify:browser` 通过 50 个 WebKit 场景，隐私检查通过。GitHub runner 外的干净 Windows 10/11 x64 用户真机与缺少 `pdftotext` 的人工降级验收仍单独延期，不由 CI 结果替代。

## Release provenance

- `v1.3.0-beta.2` 与对应 GitHub prerelease 是不可变的已发布源码快照，不移动、不删除或重建 tag。
- PR #59 的 tag 后提交属于 beta.2 开发线的交付完善；合入 `main` 时保留提交祖先关系，但不把后续提交伪装成 tag 源码。
- PR #59 合并并通过 `main` 同提交门禁后，beta.2 开发线封档；后续反馈流程只进入新的 prerelease 版本。

## Evidence

- [x] `v1.3.0-beta.2` tag 解引用到 `14993b2`；该提交的 `quality`、`documentation`、`browser` 和 `windows-core` 均通过
- [x] tag 后交付完善提交 `fdfde69` 的 `quality`、`quality-core`、`chromium`、`core-browser`、`browser`、`windows-core` 和 `documentation` 七项检查均通过
- [x] Windows runner 已验证正式离线包构建、解压、隔离 `%LOCALAPPDATA%` 安装、生产启动、仅 `127.0.0.1` 监听和按 stable ID 的重启持久化
- [x] 空库迁移、旧库升级、备份、隐私和真实数据库保护边界已验证
- [x] Core 与 Full 共用 Schema 和业务数据；未修改跨仓协议、WCG wire shape 或 no-send 边界
- [x] `package.json`、`package-lock.json`、产品变更记录、tag 与 GitHub Release 标题均使用 `1.3.0-beta.2`
- [ ] 在 GitHub-hosted runner 之外的干净 Windows 10/11 x64 真机，使用最终对外附件完成首次安装、实际 LocalAppData 路径、重复准备、启动和重启持久化验收
- [ ] 在未安装 `pdftotext` 的干净 Windows 真机确认 PDF 解析仅降级提示，不影响 Excel、人工录入和反馈主流程
