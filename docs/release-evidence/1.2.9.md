# Student Track 1.2.9 Zhuiver 发布记录

- Previous Student Track version: 1.2.8
- Student Track version: 1.2.9
- Zhuiver level: PATCH
- Zhuiver rationale: 合并课后任务中含义重叠的公共材料选择与使用控件，消除已选材料但反馈仍显示不使用的矛盾状态；不改变材料、共同课修订或计划快照模型。
- Product claim changed: no
- Core workflow changed: no
- Domain model changed: no
- Protocol contract changed: no
- Protocol issue/tag (if applicable): GitHub Issue #54
- Compatibility records: 独立课次仍保存 `ClassSession` 公共材料快照；共同课仍由主班显式确认不可变修订；FeedbackPlan 继续复制明确选择的材料或明确不使用。
- Verification evidence: 公共材料与反馈工作流 WebKit 5 条通过；完整课程周期与教师纯页面 WebKit 2 条通过；`verify:quick`、隐私与文档检查通过。

## Evidence

- [x] 准备页只显示一个“本次课程材料”选择器
- [x] 独立课次改选材料后，进入核对时保存并采用同一材料
- [x] 共同课主班仍需显式确认共享，跟随班仍可明确不使用
- [x] `npm run verify:quick` 通过
- [x] `npm run privacy:check`、`npm run docs:check` 和 `npm run docs:links` 通过
- [x] 无 Schema 变化，不执行生产 migration
- [x] 真实 SQLite 主文件与 WAL 未变化
- [x] 未修改跨仓协议、WCG no-send 或 STEP experimental 边界
