# Student Track 1.3.0-beta.3 Zhuiver 发布记录

- Previous Student Track version: 1.3.0-beta.2
- Student Track version: 1.3.0-beta.3
- Zhuiver level: MINOR
- Zhuiver rationale: 在 beta.2 的 Core / Full、Windows Core 和学期公共材料基础上，增加计划级受限/自由生成、服务端披露输入、可选课程材料、内置文字型 PDF 解析，以及 Windows Core 与 macOS Full 离线安装和保留数据卸载，属于向后兼容的产品能力扩展。
- Product claim changed: yes
- Core workflow changed: yes
- Domain model changed: yes
- Protocol contract changed: no
- Protocol issue/tag (if applicable): not applicable
- Compatibility records: Core 与 Full 继续共用同一 Prisma Schema、migration 和业务数据；本版为 `FeedbackPlan`、`FeedbackPlanBatch` 和 `FeedbackPlanItem` 增加生成方式与执行快照，历史计划以内置 `legacy` 值兼容。未修改 `docs/contracts/**`、WCG handoff v1、receipt v1、no-send 边界或协议仓；对应 GitHub Issue #61 和 PR #62。
- CI gate run (records `HEAD_SHA`): https://github.com/theodontino/student-track/actions/runs/33881790623，`HEAD_SHA=53cacf18b7b384c7cff7a44f4a9f951275247371`
- `PRODUCT_VERIFIED_SHA`: `53cacf18b7b384c7cff7a44f4a9f951275247371`
- CI level/scopes: L3 / docs, generated-docs, app, browser, build, windows, macos, database, ci, release, unknown
- Verification evidence: PR #62 的同一提交 L3 gate 共 20 项检查，19 项成功；Chromium smoke 因完整 Chromium job 已运行而按策略跳过。Core 浏览器契约、macOS Full 生产构建与离线 bundle、Windows Core 离线包、Chromium full、WebKit full、生产构建、数据库升级、备份恢复、发布隐私、文档、lint、类型检查和单元/集成测试全部通过。Windows runner 已验证离线安装、回环启动和重启持久化；macOS runner 已验证离线安装、回环启动、写入、重启读取、卸载保留数据、重新安装和再次读取。macOS ZIP 尚未签名或公证，不作为 `.app` 发布。

## Evidence

- [x] 已记录当前 CI gate；其摘要中的 `HEAD_SHA` 与发布候选一致，且全部 in-scope job 通过
- [x] `HEAD_SHA` 与 `PRODUCT_VERIFIED_SHA` 一致
- [x] 空库迁移、数据库升级、备份恢复、发布隐私和真实数据库保护边界已验证
- [x] 不涉及跨仓协议、WCG wire shape 或 no-send 边界变化
- [x] `package.json`、`package-lock.json`、产品变更记录和 README 均使用 `1.3.0-beta.3`
- [ ] GitHub merge commit 的 `main` 门禁通过后，创建 `v1.3.0-beta.3` annotated tag 与同名 prerelease
- [ ] 在 GitHub-hosted runner 之外的干净 Windows 10/11 x64 和对应架构 Mac 上，使用最终对外附件完成真实教师安装验收
