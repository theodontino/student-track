# Student Track 1.3.1 Zhuiver 发布记录

- Previous Student Track version: 1.3.0
- Student Track version: 1.3.1
- Zhuiver level: PATCH
- Zhuiver rationale: 改善既有的花名册导入、反馈 Excel 导出、助教评分回传、批量导出、转班恢复和本机维护流程，并修复 macOS Full 在没有系统 Node 时无法校验便携 npm 的安装阻断；不新增业务目标，不改变领域模型或跨仓协议。
- Product claim changed: no
- Core workflow changed: no
- Domain model changed: no
- Protocol contract changed: no
- Protocol issue/tag (if applicable): not applicable
- Compatibility records: 数据库 Schema 与 1.3.0 相同且无新增 migration；Core / Full、WCG no-send 和现有 STEP 人工文件桥边界不变。STEP 附近设备直连顺延到 1.3.2，不进入本版本。
- CI gate run (records `HEAD_SHA`): pending
- `PRODUCT_VERIFIED_SHA`: pending
- CI level/scopes: L3 / app, browser, build, windows, macos, database, release
- Verification evidence: PR #99 的 L2 候选已通过 lint、类型检查、839 项单元/集成测试、完整 Chromium/WebKit、生产构建、Windows Core 离线安装与重启持久化、文档和隐私检查。2026-09-16 用户对与合并 main 文件树一致的候选完成 dogfood，未发现问题；1.3.1 版本提交的本地与远端 L3 证据待回填。

## Evidence

- [ ] 已记录当前 CI gate；其摘要中的 `HEAD_SHA` 与发布候选一致，且全部 in-scope job 通过
- [ ] `HEAD_SHA` 与 `PRODUCT_VERIFIED_SHA` 一致，或两者之间只有已通过文档门禁的严格 L0 改动
- [ ] 迁移、备份、隐私和回滚证据已记录
- [x] 本版本不改变跨仓协议；WCG no-send 与 STEP experimental 人工文件边界保持不变
- [ ] 版本文件、tag 和 Release 标题一致

## 人工验收

- 2026-09-16：用户确认 1.3.1 功能候选 dogfood 无异常。该候选 `fc3400d065e94495dd5bcb58a24cefef42285a46` 与合并后的 `1905d285c4ea0318021d0cb31153f1f163428b41` 文件树一致。
- STEP 附近设备直连未实现、未参与本次 dogfood，也不作为 1.3.1 发布主张。

## 兼容与回滚

1.3.1 不增加 Prisma migration。回退到 1.3.0 前仍应保留数据库与运行目录备份；1.3.0 可以忽略新增的诊断故障账本文件。花名册原文件、助教模板下载和诊断包均不保存为数据库实体，回滚不会删除教学数据。

## 分发证据

待 L3 候选、`v1.3.1` 标签、GitHub Release 和验证资产发布后回填。
