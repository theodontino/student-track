# Student Track 1.3.0 Zhuiver 发布记录

- Previous Student Track version: 1.3.0-beta.8
- Student Track version: 1.3.0
- Zhuiver level: MINOR
- Zhuiver rationale: 将已完成 L3 验证的 1.3 beta 开发线正式收口为“反馈引擎与多平台分发更新”：反馈计划可组织跨班学生，生成引擎提供受限披露输入与自适应并发，Core / Full 保持同一业务模型并提供离线分发。beta.8 后补入的企微匹配与界面层级修复一并进入正式候选；不改变 WCG no-send 或 STEP experimental 边界。
- Product claim changed: yes
- Core workflow changed: yes
- Domain model changed: yes
- Protocol contract changed: no
- Protocol issue/tag (if applicable): not applicable
- Compatibility records: 从 1.3.0-beta.8 升级保持同一 Prisma Schema；旧 FeedbackPlan 与 FeedbackPlanBatch 继续只读并可复制冻结事实到新计划，既有正文、批准、导出、附件和审计记录不被覆盖。Core 继续阻断录音转写和企微/WCG，Full 保持 WCG handoff、教师批准及 no-send 草稿边界；STEP Bridge 仍为 experimental。
- CI gate run (records `HEAD_SHA`): [CI policy #34240990575](https://github.com/theodontino/student-track/actions/runs/34240990575) passed on `bdae0dd`.
- `PRODUCT_VERIFIED_SHA`: `bdae0dd7931b973aa60bd4f41739515692b6213e`.
- CI level/scopes: L3 / app, browser, build, windows, macos, database, release
- Verification evidence: 本地 `npm run verify:release` 通过 lint、类型检查、816 个测试、构建、迁移升级、隐私检查与 Chromium / WebKit 发布 E2E；远端 L3 通过相同候选提交的 Windows Core 离线包、macOS Full 生产与离线包、迁移、备份恢复、隐私、浏览器和发布门禁。标签、正式 GitHub Release 与验证资产将在下步回填。

## Evidence

- [x] 已记录当前 CI gate；其摘要中的 `HEAD_SHA` 与发布候选一致，且全部 in-scope job 通过
- [x] `HEAD_SHA` 与 `PRODUCT_VERIFIED_SHA` 一致，或两者之间只有已通过文档门禁的严格 L0 改动
- [x] 迁移、备份、隐私和回滚证据已记录
- [x] 本版本不改变跨仓协议；WCG no-send 与 STEP experimental 边界保持不变
- [ ] 版本文件、tag 和 Release 标题一致

## 兼容与回滚

升级前按运维手册创建并校验数据库备份。若需要回退到 1.3.0-beta.8 或更早版本，应保留升级后的数据库与附件目录副本，再恢复升级前经校验的备份；不得让旧程序直接写入已由新版本迁移过的数据库。回滚不会改写教师正文、批准状态或导出记录。
