# Student Track 1.3.0-beta.5 Zhuiver 发布记录

- Previous Student Track version: 1.3.0-beta.4
- Student Track version: 1.3.0-beta.5
- Zhuiver level: PATCH
- Zhuiver rationale: 在既有“录入—规划—生成”课后工作流内修复 STEP、文件夹和文字型出门测材料导入，补齐教师冲突确认与批量操作；不新增独立业务对象、部署模型或正式跨仓协议。
- Product claim changed: yes
- Core workflow changed: no
- Domain model changed: yes
- Protocol contract changed: no
- Protocol issue/tag (if applicable): not applicable
- Compatibility records: Core 与 Full 继续共用同一 Prisma Schema 和业务数据；`SessionMetric` 与 `SessionMetricHistory` 的既有整数 A 分等值迁移为 `REAL`，B/C/D、课次关系、操作人和历史记录不变。已有 STEP V1 继续可读并新增 V2 适配；未修改正式 ST↔STEP 或 ST↔WCG 契约。只更新教师最终选择的评分维度，已有其他维度不被自动覆盖。
- CI gate run (records `HEAD_SHA`): 候选分支完整 L3 CI 尚未完成；按发布授权先创建 prerelease，不把本地自动化表述为远端或实机验收
- `PRODUCT_VERIFIED_SHA`: 待候选提交完成远端产品门禁后填写
- CI level/scopes: L3 / docs, generated-docs, app, browser, build, windows, macos, database, release
- Verification evidence: 本地隔离验证通过 lint、typecheck、109 个测试文件共 767 项测试、数据库升级链、文档、隐私、生产构建、Chromium 冒烟 5 项与 WebKit 51 项（4 项按 fixture 条件跳过）；Windows Core、macOS Full 离线交付和远端 L3 仍待 CI，真实设备验收按本次发布授权免除。

## Evidence

- [ ] 已记录当前 CI gate；其摘要中的 `HEAD_SHA` 与发布候选一致，且全部 in-scope job 通过
- [ ] `HEAD_SHA` 与 `PRODUCT_VERIFIED_SHA` 一致，或两者之间只有已通过文档门禁的严格 L0 改动
- [x] 全新迁移链、固定合成旧库、受控历史反馈库和真实库副本通过完整性检查；真实 SQLite 主文件不由自动化测试写入
- [x] Chromium 冒烟与完整 WebKit 自动化在隔离副本和合成临时数据库通过；生产构建通过
- [ ] Windows Core 与 macOS Full 离线产物的构建、安装、启动和重启持久化等待远端 L3 CI
- [x] `package.json`、`package-lock.json`、README、发布兼容表、安装脚本和产品变更记录均使用 `1.3.0-beta.5`
- [ ] PR 合并后，最终产品验证提交的 L3 gate 通过并回填 `HEAD_SHA` / `PRODUCT_VERIFIED_SHA`
- [ ] 从最终产品验证提交创建 `v1.3.0-beta.5` annotated tag 与同名 GitHub prerelease；不移动旧 tag
- [x] 真实 Safari / Edge 与一次完整课后流程的实机验收由本次发布授权明确免除；自动化证据不被表述为实机验收
