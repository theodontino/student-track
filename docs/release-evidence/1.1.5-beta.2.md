# Student Track 1.1.5-beta.2 Zhuiver 发布记录

- Previous Student Track version: 1.1.5-beta
- Student Track version: 1.1.5-beta.2
- Zhuiver level: PATCH
- Zhuiver rationale: 修复项目或运行数据目录迁移后，本地转写任务仍引用旧绝对路径的兼容性阻断，并修复 STEP V2 课堂导入无法识别、模型补充解读失败会阻断教师复核以及反馈模块在窄布局下不可读的问题；这些修复仍属于原有本地课堂导入与课后反馈工作流，不改变单教师、本地优先、证据驱动的产品范式、数据所有权或跨仓协议。
- Product claim changed: no
- Core workflow changed: no
- Domain model changed: no
- Protocol contract changed: no
- Protocol issue/tag (if applicable): not applicable
- Compatibility records: WCG 0.6.0；handoff v1；receipt v1；未修改 WCG、协议快照、字段、目录结构或发送行为
- Verification evidence: `npm run verify:quick` 通过（lint、typecheck、83 个测试文件/428 个测试和真实 SQLite/WAL 未变化）；定向 STEP 导入测试 6/6 通过；`npm run privacy:check` 与 `npm run docs:check` 通过；`npm run docs:links` 仍被现有协议仓库缺少 `Zhuiver.md` 的基线问题阻断

## Evidence

- [x] `npm run verify:quick` 通过，包含本次转写任务路径迁移和 STEP V2 导入回归
- [x] STEP 课堂导入定向测试通过；模型补充解读失败时仍保留确定性课堂事实并允许教师继续复核
- [x] 隐私检查通过，未提交真实数据、运行数据或凭据
- [x] 本版本不涉及跨仓协议，无需协议 Issue/RFC 或联合契约变更
- [ ] `npm run verify:release`（现有 `docs:links` 跨仓 `Zhuiver.md` 基线链接仍待补齐）
- [ ] 版本 tag 和 Release 标题一致（本次提交与打 tag 后核对）
